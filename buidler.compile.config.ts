// import { usePlugin } from  "@nomiclabs/buidler/config";

const config = {
  solc: {
    version: '0.6.4',
    optimizer: {
      enabled: true,
      runs: 200
    }
  },
}

export default config;
