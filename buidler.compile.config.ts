// import { usePlugin } from  "@nomiclabs/buidler/config";

const config = {
  solc: {
    version: '0.4.24',
    optimizer: {
      enabled: true,
      runs: 200
    }
  },
}

export default config;
